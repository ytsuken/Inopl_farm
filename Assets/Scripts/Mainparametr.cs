using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mainparametr : MonoBehaviour
{
    public static int farm_i = 0;
    public static GameObject butFly;
    public static bool boolLoad = true;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
